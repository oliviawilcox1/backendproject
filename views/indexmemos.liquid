{% layout "layout.liquid" %}
{% block content %}

<h1> All Memos </h1>

{% for job in jobs %} 
{{job}}
<h5 class="card-header"> Memo Number : {{job.order_number}}  </h5>
    <div class="card-body">
           <a href="/memos/{{ memo._id }}" class="btn btn-light">Print Memo</a> 
    </div>
  
    <!-- {% if forloop.last %} Done{% endif %} -->
{% endfor %}
{% endblock %}